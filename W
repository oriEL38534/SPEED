local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")
local HttpService = game:GetService("HttpService")
local player = Players.LocalPlayer
local pGui = player:WaitForChild("PlayerGui")

-- ניקוי שאריות
if pGui:FindFirstChild("SpeedMenuGui") then pGui.SpeedMenuGui:Destroy() end
if pGui:FindFirstChild("BallStatsGui") then pGui.BallStatsGui:Destroy() end

-- משתני מצב
local infJumpActive, korbloxActive, headlessActive, ballStatsActive, eSpamActive = false, false, false, false, false
local currentSavedSpeed, currentSavedJump, currentSavedFOV = 30, 50, 70

-- בניית GUI
local screenGui = Instance.new("ScreenGui", pGui)
screenGui.Name = "SpeedMenuGui"
local frame = Instance.new("Frame", screenGui)
frame.Size, frame.Position, frame.BackgroundColor3 = UDim2.new(0, 480, 0, 540), UDim2.new(0.5, -240, 0.15, 0), Color3.fromRGB(22, 18, 29)
frame.Active, frame.Draggable = true, true
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", frame).Color = Color3.fromRGB(140, 50, 200)

local leftCol = Instance.new("Frame", frame)
leftCol.Size, leftCol.Position, leftCol.BackgroundTransparency = UDim2.new(0.46, 0, 0.85, 0), UDim2.new(0.03, 0, 0.1, 0), 1
Instance.new("UIListLayout", leftCol).Padding = UDim.new(0, 6)

local rightCol = Instance.new("Frame", frame)
rightCol.Size, rightCol.Position, rightCol.BackgroundTransparency = UDim2.new(0.46, 0, 0.85, 0), UDim2.new(0.51, 0, 0.1, 0), 1
Instance.new("UIListLayout", rightCol).Padding = UDim.new(0, 6)

local function createButton(parent, text, onClick)
    local btn = Instance.new("TextButton", parent)
    btn.Size, btn.Text, btn.TextColor3 = UDim2.new(1, 0, 0, 32), text, Color3.new(1, 1, 1)
    btn.BackgroundColor3, btn.BorderSizePixel = Color3.fromRGB(35, 30, 48), 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    btn.MouseButton1Click:Connect(function() onClick(btn) end)
    return btn
end

-- 400Hz E-SPAM (המנגנון המבוקש)
local eSpamBtn = createButton(leftCol, "⚡ 400Hz E-Spam: כבוי", function(btn)
    eSpamActive = not eSpamActive
    btn.Text = eSpamActive and "✅ 400Hz E-Spam: פעיל" or "⚡ 400Hz E-Spam: כבוי"
    btn.BackgroundColor3 = eSpamActive and Color3.fromRGB(200, 50, 50) or Color3.fromRGB(35, 30, 48)
end)

RunService.Heartbeat:Connect(function()
    if eSpamActive then
        pcall(function()
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.E, false, game)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.E, false, game)
        end)
    end
end)

-- הוספת שאר הפיצ'רים המקוריים
createButton(leftCol, "♾️ Inf Jump", function() infJumpActive = not infJumpActive end)
createButton(rightCol, "🦵 Korblox", function() korbloxActive = not korbloxActive end)
createButton(rightCol, "💀 Headless", function() headlessActive = not headlessActive end)

print("Script Loaded Successfully.")
