local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")
local player = Players.LocalPlayer

-- משתנים לניהול מצב
local infJumpActive = false
local autoParryActive = false
local autoParryConnection = nil
local highlightsFolder = nil
local createdLight = nil

-- ניקוי ממשק ישן אם קיים
local pGui = player:WaitForChild("PlayerGui")
if pGui:FindFirstChild("SpeedMenuGui") then
	pGui.SpeedMenuGui:Destroy()
end

-- יצירת הממשק הוויזואלי (ScreenGui)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SpeedMenuGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = pGui

-- יצירת המסגרת הראשית (הגדלתי מעט את הגובה בשביל Auto Parry)
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 460)
frame.Position = UDim2.new(0.05, 0, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
frame.BorderSizePixel = 1
frame.BorderColor3 = Color3.fromRGB(50, 50, 50)
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 8)
frameCorner.Parent = frame

-- כותרת
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -30, 0, 40)
title.Position = UDim2.new(0, 12, 0, 0)
title.Text = "תפריט שליטה"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.BackgroundTransparency = 1
title.TextSize = 16
title.Font = Enum.Font.SourceSansBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

-- כפתור סגירה (X)
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 40)
closeBtn.Position = UDim2.new(1, -30, 0, 0)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
closeBtn.BackgroundTransparency = 1
closeBtn.TextSize = 16
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.Parent = frame

-- כפתור צף להצגה/הסתרה
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 85, 0, 35)
toggleBtn.Position = UDim2.new(0.02, 0, 0.2, 0)
toggleBtn.Text = "⚡ תפריט"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
toggleBtn.BorderSizePixel = 1
toggleBtn.BorderColor3 = Color3.fromRGB(60, 60, 60)
toggleBtn.TextSize = 14
toggleBtn.Font = Enum.Font.SourceSansBold
toggleBtn.Active = true
toggleBtn.Draggable = true
toggleBtn.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 6)
toggleCorner.Parent = toggleBtn

local function toggleMenu()
	frame.Visible = not frame.Visible
end

closeBtn.MouseButton1Click:Connect(toggleMenu)
toggleBtn.MouseButton1Click:Connect(toggleMenu)

-- פונקציה לעדכון מהירות
local function setSpeed(newSpeed)
	local character = player.Character or player.CharacterAdded:Wait()
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if humanoid then
		humanoid.WalkSpeed = newSpeed
	end
end

-- פונקציית עזר ליצירת כפתורים
local function createButton(text, positionY, onClick)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0.86, 0, 0, 32)
	btn.Position = UDim2.new(0.07, 0, 0, positionY)
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(240, 240, 240)
	btn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
	btn.BorderSizePixel = 0
	btn.TextSize = 14
	btn.Font = Enum.Font.SourceSansSemibold
	btn.Parent = frame

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 5)
	btnCorner.Parent = btn

	btn.MouseButton1Click:Connect(function()
		onClick(btn)
	end)
	return btn
end

-- ניקוי הילות
local function removeHighlights()
	if highlightsFolder then
		highlightsFolder:Destroy()
		highlightsFolder = nil
	end
	if createdLight then
		createdLight:Destroy()
		createdLight = nil
	end
	for _, obj in ipairs(Workspace:GetDescendants()) do
		if obj:IsA("Highlight") and (obj.Name == "BallSuperGlow" or obj.Name:find("_Glow") or obj.Name:find("_BlackAura")) then
			obj:Destroy()
		end
		if obj:IsA("PointLight") and obj.Name == "BallLight" then
			obj:Destroy()
		end
	end
end

-- איתור הכדור
local function findBall()
	local names = {"Ball", "Football", "SoccerBall", "TPSBall"}
	for _, v in ipairs(Workspace:GetDescendants()) do
		if v:IsA("BasePart") then
			for _, name in ipairs(names) do
				if v.Name:lower():find(name:lower()) then
					return v
				end
			end
		end
	end
	return nil
end

-- יצירת הילות למצב כהה
local function applyDarkHighlights()
	removeHighlights()
	
	highlightsFolder = Instance.new("Folder")
	highlightsFolder.Name = "GlowHighlights"
	highlightsFolder.Parent = Workspace

	local targetBall = findBall()
	if targetBall then
		local ballHighlight = Instance.new("Highlight")
		ballHighlight.Name = "BallSuperGlow"
		ballHighlight.FillColor = Color3.fromRGB(255, 120, 0)
		ballHighlight.FillTransparency = 0.1
		ballHighlight.OutlineColor = Color3.fromRGB(255, 255, 0)
		ballHighlight.OutlineTransparency = 0
		ballHighlight.Parent = highlightsFolder
		ballHighlight.Adornee = targetBall

		local light = Instance.new("PointLight")
		light.Name = "BallLight"
		light.Color = Color3.fromRGB(255, 140, 0)
		light.Range = 35
		light.Brightness = 10
		light.Parent = targetBall
		createdLight = light
	end

	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= player and plr.Character then
			local plrHighlight = Instance.new("Highlight")
			plrHighlight.Name = plr.Name .. "_Glow"
			plrHighlight.FillColor = Color3.fromRGB(0, 180, 255)
			plrHighlight.FillTransparency = 0.5
			plrHighlight.OutlineColor = Color3.fromRGB(255, 255, 255)
			plrHighlight.OutlineTransparency = 0
			plrHighlight.Parent = highlightsFolder
			plrHighlight.Adornee = plr.Character
		end
	end
end

-- יצירת הילה שחורה כהה לשחקנים ולכדור
local function applyBlackAura()
	removeHighlights()
	
	highlightsFolder = Instance.new("Folder")
	highlightsFolder.Name = "BlackAuraHighlights"
	highlightsFolder.Parent = Workspace

	local targetBall = findBall()
	if targetBall then
		local ballBlackHighlight = Instance.new("Highlight")
		ballBlackHighlight.Name = "Ball_BlackAura"
		ballBlackHighlight.FillColor = Color3.fromRGB(0, 0, 0)
		ballBlackHighlight.FillTransparency = 0.05
		ballBlackHighlight.OutlineColor = Color3.fromRGB(0, 0, 0)
		ballBlackHighlight.OutlineTransparency = 0
		ballBlackHighlight.Parent = highlightsFolder
		ballBlackHighlight.Adornee = targetBall
	end

	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= player and plr.Character then
			local blackHighlight = Instance.new("Highlight")
			blackHighlight.Name = plr.Name .. "_BlackAura"
			blackHighlight.FillColor = Color3.fromRGB(0, 0, 0)
			blackHighlight.FillTransparency = 0.05
			blackHighlight.OutlineColor = Color3.fromRGB(0, 0, 0)
			blackHighlight.OutlineTransparency = 0
			blackHighlight.Parent = highlightsFolder
			blackHighlight.Adornee = plr.Character
		end
	end
end

-- כפתורי מהירות
createButton("מהירות 30", 45, function() setSpeed(30) end)
createButton("מהירות 100", 85, function() setSpeed(100) end)
createButton("מהירות 200", 125, function() setSpeed(200) end)

-- כפתור Infinite Jump
local infJumpBtn = createButton("🦘 Infinite Jump: כבוי", 170, function() end)

-- מנגנון Infinite Jump
local ACTION_NAME = "InfJumpAction"

local function handleJumpAction(actionName, inputState, inputObject)
	if inputState == Enum.UserInputState.Begin then
		if infJumpActive then
			local char = player.Character
			if char then
				local hum = char:FindFirstChildOfClass("Humanoid")
				if hum then
					hum:ChangeState(Enum.HumanoidStateType.Jumping)
				end
			end
		end
	end
	return Enum.ContextAnimationFrame.Pass
end

local function disableInfJump()
	infJumpActive = false
	ContextActionService:UnbindAction(ACTION_NAME)
	infJumpBtn.Text = "🦘 Infinite Jump: כבוי"
	infJumpBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
end

local function enableInfJump()
	disableInfJump()
	infJumpActive = true
	ContextActionService:BindAction(ACTION_NAME, handleJumpAction, false, Enum.KeyCode.Space)
	infJumpBtn.Text = "✅ Infinite Jump: פעיל"
	infJumpBtn.BackgroundColor3 = Color3.fromRGB(40, 90, 40)
end

infJumpBtn.MouseButton1Click:Connect(function()
	if infJumpActive then disableInfJump() else enableInfJump() end
end)

-- כפתור AUTO PARRY
local autoParryBtn = createButton("⚔️ Auto Parry: כבוי", 215, function() end)

-- לוגיקת AUTO PARRY בזמן אמת
local function performParry()
	-- שליחת מקש F / לחיצה שמבצעת Parry במשחק
	VirtualInputManager:SendKeyPressEvent(Enum.KeyCode.F, false, game)
	VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
	task.wait(0.01)
	VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
end

local function disableAutoParry()
	autoParryActive = false
	if autoParryConnection then
		autoParryConnection:Disconnect()
		autoParryConnection = nil
	end
	autoParryBtn.Text = "⚔️ Auto Parry: כבוי"
	autoParryBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
end

local function enableAutoParry()
	disableAutoParry()
	autoParryActive = true
	autoParryBtn.Text = "✅ Auto Parry: פעיל"
	autoParryBtn.BackgroundColor3 = Color3.fromRGB(40, 90, 40)

	autoParryConnection = RunService.PreRender:Connect(function()
		if not autoParryActive then return end

		local char = player.Character
		if not char then return end
		local root = char:FindFirstChild("HumanoidRootPart")
		if not root then return end

		local ball = findBall()
		if not ball then return end

		-- חישוב מרחק ומהירות הכדור אל השחקן
		local ballPos = ball.Position
		local ballVel = ball.AssemblyLinearVelocity
		local distance = (ballPos - root.Position).Magnitude
		local speed = ballVel.Magnitude

		-- סף מרחק דינמי מבוסס מהירות
		local parryDistance = math.clamp(speed * 0.4, 15, 60)

		if distance <= parryDistance then
			-- בדיקה שהכדור אכן נע לכיוון השחקן
			local dotProduct = (root.Position - ballPos).Unit:Dot(ballVel.Unit)
			if dotProduct > 0.2 or distance < 18 then
				performParry()
			end
		end
	end)
end

autoParryBtn.MouseButton1Click:Connect(function()
	if autoParryActive then disableAutoParry() else enableAutoParry() end
end)

-- שליטה בגרפיקת המשחק
createButton("🌒 כהה (עם כדור זרחני)", 260, function()
	Lighting.ClockTime = 20
	Lighting.Brightness = 1.2
	Lighting.GlobalShadows = true
	Lighting.OutdoorAmbient = Color3.fromRGB(70, 70, 90)
	Lighting.Ambient = Color3.fromRGB(60, 60, 80)
	
	applyDarkHighlights()
end)

createButton("☀️ בהיר (אורה שחורה כהה)", 305, function()
	Lighting.ClockTime = 14
	Lighting.Brightness = 3
	Lighting.GlobalShadows = false
	Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
	Lighting.Ambient = Color3.fromRGB(255, 255, 255)
	Lighting.FogEnd = 100000
	
	applyBlackAura()
end)

createButton("🔄 החזר גרפיקה לרגיל", 350, function()
	removeHighlights()
	Lighting.ClockTime = 14.5
	Lighting.Brightness = 2
	Lighting.GlobalShadows = true
	Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
	Lighting.Ambient = Color3.fromRGB(70, 70, 70)
	Lighting.FogEnd = 10000
end)

-- כפתור איפוס הכל
local resetAllBtn = createButton("🛑 איפוס הכל (מהירות+קפיצה+AutoParry)", 395, function() end)
resetAllBtn.BackgroundColor3 = Color3.fromRGB(130, 35, 35)

resetAllBtn.MouseButton1Click:Connect(function()
	removeHighlights()
	disableInfJump()
	disableAutoParry()
	setSpeed(16)
end)
