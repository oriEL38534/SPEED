local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local player = Players.LocalPlayer

local pGui = player:WaitForChild("PlayerGui")

-- ניקוי שאריות קודמות
if pGui:FindFirstChild("ScriptIntroGui") then pGui.ScriptIntroGui:Destroy() end
if pGui:FindFirstChild("SpeedMenuGui") then pGui.SpeedMenuGui:Destroy() end

----------------------------------------------------
-- 1. יצירת הממשק הראשי (רחב ומעוצב)
----------------------------------------------------
local infJumpActive = false
local korbloxActive = false
local headlessActive = false

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SpeedMenuGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = pGui

-- חלון רחב (460 פיקסלים ברוחב)
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 460, 0, 360)
frame.Position = UDim2.new(0.5, -230, 0.25, 0)
frame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Visible = false
frame.Parent = screenGui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 10)
frameCorner.Parent = frame

local frameStroke = Instance.new("UIStroke")
frameStroke.Color = Color3.fromRGB(45, 45, 60)
frameStroke.Thickness = 1.5
frameStroke.Parent = frame

-- כותרת
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -40, 0, 40)
title.Position = UDim2.new(0, 15, 0, 5)
title.Text = "⚡ SPEED & JUMP HUB"
title.TextColor3 = Color3.fromRGB(0, 170, 255)
title.BackgroundTransparency = 1
title.TextSize = 17
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 35, 0, 35)
closeBtn.Position = UDim2.new(1, -40, 0, 5)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
closeBtn.BackgroundTransparency = 1
closeBtn.TextSize = 18
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = frame

-- כפתור פתיחה/סגירה צף
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 90, 0, 35)
toggleBtn.Position = UDim2.new(0.02, 0, 0.2, 0)
toggleBtn.Text = "⚡ תפריט"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
toggleBtn.BorderSizePixel = 0
toggleBtn.TextSize = 14
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.Active = true
toggleBtn.Draggable = true
toggleBtn.Visible = false
toggleBtn.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 8)
toggleCorner.Parent = toggleBtn

local toggleStroke = Instance.new("UIStroke")
toggleStroke.Color = Color3.fromRGB(0, 170, 255)
toggleStroke.Thickness = 1
toggleStroke.Parent = toggleBtn

local function toggleMenu()
	frame.Visible = not frame.Visible
end

closeBtn.MouseButton1Click:Connect(toggleMenu)
toggleBtn.MouseButton1Click:Connect(toggleMenu)

----------------------------------------------------
-- 2. עמודות לוח הבקרה (ימין ושמאל)
----------------------------------------------------
local leftCol = Instance.new("Frame")
leftCol.Size = UDim2.new(0.47, 0, 0.82, 0)
leftCol.Position = UDim2.new(0.03, 0, 0.14, 0)
leftCol.BackgroundTransparency = 1
leftCol.Parent = frame

local rightCol = Instance.new("Frame")
rightCol.Size = UDim2.new(0.47, 0, 0.82, 0)
rightCol.Position = UDim2.new(0.50, 0, 0.14, 0)
rightCol.BackgroundTransparency = 1
rightCol.Parent = frame

----------------------------------------------------
-- 3. פונקציות תנועה
----------------------------------------------------
local function setSpeed(newSpeed)
	local char = player.Character or player.CharacterAdded:Wait()
	local hum = char:FindFirstChildOfClass("Humanoid")
	if hum then hum.WalkSpeed = newSpeed end
end

local function setJumpPower(newPower)
	local char = player.Character or player.CharacterAdded:Wait()
	local hum = char:FindFirstChildOfClass("Humanoid")
	if hum then
		hum.UseJumpPower = true
		hum.JumpPower = newPower
	end
end

----------------------------------------------------
-- 4. בנאי רכיבים (Sliders & Buttons)
----------------------------------------------------
local function createSlider(parent, titleText, minVal, maxVal, defaultVal, positionY, onValueChange)
	local sliderLabel = Instance.new("TextLabel")
	sliderLabel.Size = UDim2.new(1, 0, 0, 18)
	sliderLabel.Position = UDim2.new(0, 0, 0, positionY)
	sliderLabel.Text = titleText .. ": " .. tostring(defaultVal)
	sliderLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
	sliderLabel.BackgroundTransparency = 1
	sliderLabel.TextSize = 13
	sliderLabel.Font = Enum.Font.GothamMedium
	sliderLabel.TextXAlignment = Enum.TextXAlignment.Left
	sliderLabel.Parent = parent

	local sliderBG = Instance.new("TextButton")
	sliderBG.Text = ""
	sliderBG.AutoButtonColor = false
	sliderBG.Size = UDim2.new(1, 0, 0, 12)
	sliderBG.Position = UDim2.new(0, 0, 0, positionY + 22)
	sliderBG.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
	sliderBG.BorderSizePixel = 0
	sliderBG.Parent = parent

	local bgCorner = Instance.new("UICorner")
	bgCorner.CornerRadius = UDim.new(1, 0)
	bgCorner.Parent = sliderBG

	local fill = Instance.new("Frame")
	local startScale = (defaultVal - minVal) / (maxVal - minVal)
	fill.Size = UDim2.new(math.clamp(startScale, 0, 1), 0, 1, 0)
	fill.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	fill.BorderSizePixel = 0
	fill.Parent = sliderBG

	local fillCorner = Instance.new("UICorner")
	fillCorner.CornerRadius = UDim.new(1, 0)
	fillCorner.Parent = fill

	local isDragging = false

	local function updateValue(input)
		local posX = input.Position.X - sliderBG.AbsolutePosition.X
		local scale = math.clamp(posX / sliderBG.AbsoluteSize.X, 0, 1)
		fill.Size = UDim2.new(scale, 0, 1, 0)
		
		local val = math.floor(minVal + (scale * (maxVal - minVal)))
		sliderLabel.Text = titleText .. ": " .. tostring(val)
		onValueChange(val)
	end

	sliderBG.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			isDragging = true
			updateValue(input)
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			isDragging = false
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			updateValue(input)
		end
	end)
end

local function createButton(parent, text, positionY, onClick)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, 0, 0, 32)
	btn.Position = UDim2.new(0, 0, 0, positionY)
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(240, 240, 240)
	btn.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
	btn.BorderSizePixel = 0
	btn.TextSize = 13
	btn.Font = Enum.Font.GothamMedium
	btn.Parent = parent

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = btn

	btn.MouseButton1Click:Connect(function() onClick(btn) end)
	return btn
end

----------------------------------------------------
-- 5. הוספת אלמנטים - עמודה ימנית (מהירות וקפיצה)
----------------------------------------------------
createSlider(leftCol, "⚡ מהירות (Speed)", 30, 200, 30, 10, function(val)
	setSpeed(val)
end)

createSlider(leftCol, "🦘 גובה קפיצה (Jump)", 30, 200, 50, 60, function(val)
	setJumpPower(val)
end)

-- Infinite Jump
local infJumpBtn = createButton(leftCol, "♾️ Infinite Jump: כבוי", 115, function() end)

local ACTION_NAME = "InfJumpAction"

local function handleJumpAction(actionName, inputState, inputObject)
	if inputState == Enum.UserInputState.Begin and infJumpActive then
		local char = player.Character
		if char then
			local hum = char:FindFirstChildOfClass("Humanoid")
			if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
		end
	end
	return Enum.ContextAnimationFrame.Pass
end

local function disableInfJump()
	infJumpActive = false
	ContextActionService:UnbindAction(ACTION_NAME)
	infJumpBtn.Text = "♾️ Infinite Jump: כבוי"
	infJumpBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
end

local function enableInfJump()
	disableInfJump()
	infJumpActive = true
	ContextActionService:BindAction(ACTION_NAME, handleJumpAction, false, Enum.KeyCode.Space)
	infJumpBtn.Text = "✅ Infinite Jump: פעיל"
	infJumpBtn.BackgroundColor3 = Color3.fromRGB(35, 110, 50)
end

infJumpBtn.MouseButton1Click:Connect(function()
	if infJumpActive then disableInfJump() else enableInfJump() end
end)

-- כפתור איפוס הכל בעמודה ימין
local resetAllBtn = createButton(leftCol, "🛑 איפוס הכל", 160, function() end)
resetAllBtn.BackgroundColor3 = Color3.fromRGB(120, 30, 30)

----------------------------------------------------
-- 6. הוספת אלמנטים - עמודה שמאלית (קוסמטיקה ותאורה)
----------------------------------------------------
local function applyKorblox()
	pcall(function()
		local char = player.Character
		if not char then return end
		local rightLegR6 = char:FindFirstChild("Right Leg")
		if rightLegR6 then
			local mesh = char:FindFirstChildOfClass("CharacterMesh")
			if not mesh or mesh.BodyPart ~= Enum.BodyPart.RightLeg then
				mesh = Instance.new("CharacterMesh")
				mesh.BodyPart = Enum.BodyPart.RightLeg
				mesh.Parent = char
			end
			mesh.MeshId = 90294209
			rightLegR6.Transparency = 0
		else
			local rightLower = char:FindFirstChild("RightLowerLeg")
			local rightFoot = char:FindFirstChild("RightFoot")
			if rightLower then
				local mesh = rightLower:FindFirstChildOfClass("SpecialMesh") or Instance.new("SpecialMesh", rightLower)
				mesh.MeshId = "rbxassetid://90294209"
				mesh.TextureId = "rbxassetid://90284029"
			end
			if rightFoot then rightFoot.Transparency = 1 end
		end
	end)
end

local function applyHeadless()
	pcall(function()
		local char = player.Character
		if not char then return end
		local head = char:FindFirstChild("Head")
		if head then
			head.Transparency = 1
			local face = head:FindFirstChildOfClass("Decal")
			if face then face.Transparency = 1 end
		end
	end)
end

local korbloxBtn = createButton(rightCol, "🦵 Korblox Leg: כבוי", 10, function() end)
korbloxBtn.MouseButton1Click:Connect(function()
	korbloxActive = not korbloxActive
	if korbloxActive then
		korbloxBtn.Text = "✅ Korblox Leg: פעיל"
		korbloxBtn.BackgroundColor3 = Color3.fromRGB(35, 110, 50)
		applyKorblox()
	else
		korbloxBtn.Text = "🦵 Korblox Leg: כבוי"
		korbloxBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
	end
end)

local headlessBtn = createButton(rightCol, "💀 Headless: כבוי", 50, function() end)
headlessBtn.MouseButton1Click:Connect(function()
	headlessActive = not headlessActive
	if headlessActive then
		headlessBtn.Text = "✅ Headless: פעיל"
		headlessBtn.BackgroundColor3 = Color3.fromRGB(35, 110, 50)
		applyHeadless()
	else
		headlessBtn.Text = "💀 Headless: כבוי"
		headlessBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
	end
end)

createButton(rightCol, "🌒 מצב כהה", 90, function()
	Lighting.ClockTime = 20
	Lighting.Brightness = 1.2
	Lighting.GlobalShadows = true
end)

createButton(rightCol, "☀️ מצב בהיר", 130, function()
	Lighting.ClockTime = 14
	Lighting.Brightness = 3
	Lighting.GlobalShadows = false
end)

createButton(rightCol, "🔄 איפוס גרפיקה", 170, function()
	Lighting.ClockTime = 14.5
	Lighting.Brightness = 2
	Lighting.GlobalShadows = true
end)

resetAllBtn.MouseButton1Click:Connect(function()
	disableInfJump()
	korbloxActive = false
	headlessActive = false
	korbloxBtn.Text = "🦵 Korblox Leg: כבוי"
	korbloxBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
	headlessBtn.Text = "💀 Headless: כבוי"
	headlessBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
	setSpeed(16)
	setJumpPower(50)
end)

player.CharacterAdded:Connect(function(char)
	char:WaitForChild("Humanoid")
	task.wait(0.5)
	if korbloxActive then applyKorblox() end
	if headlessActive then applyHeadless() end
end)

----------------------------------------------------
-- 7. מסך פתיחה (Intro)
----------------------------------------------------
task.spawn(function()
	local introGui = Instance.new("ScreenGui")
	introGui.Name = "ScriptIntroGui"
	introGui.ResetOnSpawn = false
	introGui.Parent = pGui

	local introFrame = Instance.new("Frame")
	introFrame.Size = UDim2.new(0, 340, 0, 140)
	introFrame.Position = UDim2.new(0.5, 0, 0.45, 0)
	introFrame.AnchorPoint = Vector2.new(0.5, 0.5)
	introFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
	introFrame.BorderSizePixel = 0
	introFrame.ClipsDescendants = true
	introFrame.Parent = introGui

	local introCorner = Instance.new("UICorner")
	introCorner.CornerRadius = UDim.new(0, 14)
	introCorner.Parent = introFrame

	local introStroke = Instance.new("UIStroke")
	introStroke.Color = Color3.fromRGB(0, 170, 255)
	introStroke.Thickness = 2.5
	introStroke.Parent = introFrame

	local introLabel = Instance.new("TextLabel")
	introLabel.Size = UDim2.new(1, 0, 0.35, 0)
	introLabel.Position = UDim2.new(0, 0, 0.1, 0)
	introLabel.Text = "⚡ MY-SCRIPT ⚡"
	introLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	introLabel.BackgroundTransparency = 1
	introLabel.TextSize = 26
	introLabel.Font = Enum.Font.GothamBold
	introLabel.Parent = introFrame

	local subLabel = Instance.new("TextLabel")
	subLabel.Size = UDim2.new(1, 0, 0.2, 0)
	subLabel.Position = UDim2.new(0, 0, 0.45, 0)
	subLabel.Text = "INITIALIZING..."
	subLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
	subLabel.BackgroundTransparency = 1
	subLabel.TextSize = 13
	subLabel.Font = Enum.Font.GothamMedium
	subLabel.Parent = introFrame

	local barBackground = Instance.new("Frame")
	barBackground.Size = UDim2.new(0.85, 0, 0, 8)
	barBackground.Position = UDim2.new(0.075, 0, 0.75, 0)
	barBackground.BackgroundColor3 = Color3.fromRGB(20, 25, 35)
	barBackground.BorderSizePixel = 0
	barBackground.Parent = introFrame

	local barFill = Instance.new("Frame")
	barFill.Size = UDim2.new(0, 0, 1, 0)
	barFill.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	barFill.BorderSizePixel = 0
	barFill.Parent = barBackground

	local rgbConnection
	rgbConnection = RunService.RenderStepped:Connect(function()
		if introGui and introGui.Parent then
			local hue = (tick() % 3) / 3
			introStroke.Color = Color3.fromHSV(hue, 0.8, 1)
			barFill.BackgroundColor3 = Color3.fromHSV(hue, 0.8, 1)
		else
			if rgbConnection then rgbConnection:Disconnect() end
		end
	end)

	task.wait(0.4)
	subLabel.Text = "LOADING UI MODULES [35%]"
	TweenService:Create(barFill, TweenInfo.new(0.6), {Size = UDim2.new(0.35, 0, 1, 0)}):Play()

	task.wait(0.6)
	subLabel.Text = "BUILDING HORIZONTAL HUB [75%]"
	TweenService:Create(barFill, TweenInfo.new(0.6), {Size = UDim2.new(0.75, 0, 1, 0)}):Play()

	task.wait(0.6)
	subLabel.Text = "WELCOME, " .. player.Name:upper() .. "!"
	TweenService:Create(barFill, TweenInfo.new(0.3), {Size = UDim2.new(1, 0, 1, 0)}):Play()

	task.wait(0.5)
	if rgbConnection then rgbConnection:Disconnect() end
	introGui:Destroy()

	frame.Visible = true
	toggleBtn.Visible = true
end)
